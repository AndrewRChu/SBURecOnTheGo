import React from 'react';
import { StyleSheet, View, Text, TextInput, Button, Alert, Share } from 'react-native';

const SettingsScreen = () => {
    return (
      <View>
      </View>
    )
  }

export default SettingsScreen